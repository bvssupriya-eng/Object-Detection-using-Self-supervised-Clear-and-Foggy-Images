# Workspace Local Config

This folder stores local runtime configuration for tools that normally write into the user profile.

- `ultralytics/` stores Ultralytics settings
- `jupyter/` stores local Jupyter data when needed
