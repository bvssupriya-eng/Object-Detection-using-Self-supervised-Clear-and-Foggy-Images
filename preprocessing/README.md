# Preprocessing

This folder stores all outputs produced during data preprocessing:

- `source_yolo/`
  converted YOLO-format dataset

- `label_validation/`
  images with drawn labels used to verify the conversion

- `preprocessing_summary.md`
  written summary of the preprocessing step
